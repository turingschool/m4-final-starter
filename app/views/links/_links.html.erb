<div class="row" id="my-links">
  <% current_user.links.each do |link| %>
    <div class="col-md-4">
      <p>Title: <%= link.title %></p>
      <p>URL: <%= link.url %></p>
      <p>Read? <%= link.read %></p>
      <br>
    </div>
  <% end %>
</div>
