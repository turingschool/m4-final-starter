<h2>Edit <%= @link.title %> Link</h2>

<%= form_for @link, as: :link, url: link_path(@link), :method => "put" do |f| %>
<%= f.label 'URL'  %>
<%= f.url_field :url %>
<%= f.label 'Title' %>
<%= f.text_field :title %>
<%= f.submit 'Edit Link' %>
<% end %>
