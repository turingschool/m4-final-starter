<%= form_for @link, url: links_path(@link) do |f| %>
  <%= f.text_field :title, placeholder: "Type here link title" %>
  <%= f.text_field :url, placeholder: "Type here link URL" %>
  <%= f.hidden_field :id %>
  <%= f.submit 'Save Link'%>
<% end %>