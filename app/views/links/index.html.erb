<h1>All Links</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Url</th>
  </tr>
  <% @user.links.each do |link| %>
  <tr>
    <td><%= link.title %>
    <td><%= link.url %>
  </tr>
  <% end %>
</table>
