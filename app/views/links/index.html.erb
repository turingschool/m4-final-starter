<%= link_to "Sign Out", logout_path, method: "DELETE" %>
<div class="message"><h4>Url must be valid and title cannot be blank.</h4></div>
<%= render partial: "form" %>
<div class="links">
  <% @links.each do |link| %>
    <%= render partial: "link", :locals => {link: link} %>
  <% end %>
</div>