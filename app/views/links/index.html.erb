<%= link_to "Add a Link", new_link_path %>

<hr>
<% current_user.links.each do |link| %>
  URL: <%= link.url %><br>
  <%= link.title %><br>
  <%= link.read %><br>
  <% link_to "Edit", edit_link_path(link)  %>
  <hr>
<% end %>
