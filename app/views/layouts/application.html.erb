<!DOCTYPE html>
<html>
<head>
  <title>URLockBox</title>
  <%= stylesheet_link_tag    'application', media: 'all'%>
  <%= javascript_include_tag 'application'%>
  <%= csrf_meta_tags %>
</head>

<body>
  <div class="">
    <% if current_user %>
    Signed in as <%= current_user.email %> | <%= link_to "Logout", '/logout' %>
    <% else %>
    <!-- Possibley useful links if navbar is implemented -->
    <% end %>

  </div>
<div class="container">
  <% flash[:errors].each do |value| %>
  <%= content_tag :div, value %>
<% end %>
  <%= yield %>
</div>

</body>
</html>
