<% if @errors %>
    <h3><%= pluralize(@errors.count, 'error') %> prevented this record from being saved.</h3>
    <ul>
      <% @errors.full_messages.each do |message| %>
        <li class='errors'><%= message %></li>
      <% end %>
    </ul>
<% end %>
